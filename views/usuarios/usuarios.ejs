<%- include("../templates/header") %>

  <!-- Main Content -->
  <div id="content">
    <div class="container mt-5">
      <h5 class="card-title">Gestión usuarios</h5>
      <div class="row">
        <div class="col-lg-8">
          <table class="table table-bordered table-hover">
            <thead>
              <tr>
                <td>N°</td>
                <td>Nombre</td>
                <td>Correo</td>
                <td>Rol</td>
              </tr>
            </thead>
            <tbody>
              <% if (usuarios) { %> <% for(var i = 0; i < usuarios.length; i++) {
              %>
              <tr>
                <td><%= (i + 1) %></td>
                <td><%= usuarios[i].nombre %></td>
                <td><%= usuarios[i].correo %></td>
                <td><%= usuarios[i].rolAsociado.nombre %></td>
                <td class="d-flex justify-content-around">
                  <a href="usuarios/obtener/<%= usuarios[i].id %>" class="btn btn-info">
                    Edit
                  </a>
                  <a href="usuarios/eliminar/<%= usuarios[i].id %>" class="btn btn-danger">
                    Delete
                  </a>
                </td>
              </tr>
              <% } %> <% } %>
            </tbody>
          </table>
        </div>
        <div class="col-lg-4">
          <div class="card-body">
            <form action="usuarios/agregar" method="POST">
              <input
                type="text"
                name="nombre"
                placeholder="Fulano"
                class="form-control mb-3"
                autofocus
              />
              <input
                type="email"
                name="correo"
                placeholder="correo@correo.com"
                class="form-control mb-3"
              />
    
              <button type="submit" class="btn btn-info">Guardar cliente</button>
            </form>
          </div>
        </div>
      </div>
    </div>

  </div>
  <!-- End of Main Content -->

           

   
<%- include("../templates/footer") %>


